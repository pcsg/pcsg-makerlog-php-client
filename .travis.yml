language: php

matrix:
  include:
    - php: 7.1
    - php: 7.2
    - php: 7.3
    - php: 7.3
      env: deps=high
    - php: nightly
  fast_finish: true
  allow_failures:
    - php: nightlys

before_install:
  - sudo apt-get update > /dev/null

script:
  - phpunit --bootstrap tests/bootstrap.php tests/